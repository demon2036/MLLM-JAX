# Copy this file to `.env` and fill in secrets locally (do not commit `.env`).

# Weights & Biases API key (https://wandb.ai/authorize)
WANDB_API_KEY=

# Optional defaults for this repo (non-hyperparam)
TOKENIZERS_PARALLELISM=false

