"""DEPRECATED: use `plugins.training.core` / `plugins.sample`.

`plugins.common` is kept temporarily as an import-compatibility shim.

New code should prefer:
- training infra: `plugins.training.core.*`
- sampling / generation: `plugins.sample.*`
"""
