"""Shared abstractions used across plugin modules.

This package is intentionally small: it hosts utilities that are shared between
`plugins/sft/` and `plugins/training/` so we don't have to merge those folders.
"""

