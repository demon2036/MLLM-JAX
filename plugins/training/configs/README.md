# Legacy configs (compatibility)

Canonical RL configs now live under:

- `plugins/training/rl/configs/`

This folder is kept temporarily so older SOPs/commands referencing
`plugins/training/configs/*.yaml` keep working during the refactor window.

