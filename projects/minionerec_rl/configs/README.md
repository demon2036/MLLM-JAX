# MiniOneRec SID RL configs

These YAML files are MiniOneRec/SID RL (GRPO-style) project configs.

They are tracked in Git so W&B runs can be reproduced by referencing an exact
committed config path.

