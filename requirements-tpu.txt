# Python deps for running GRPO/GSM8K training on TPU.
#
# Install JAX for TPU separately (needs the libtpu wheel index), e.g.:
#   pip install -U "jax[tpu]" -f https://storage.googleapis.com/jax-releases/libtpu_releases.html
#
# Then:
#   pip install -U -r requirements-tpu.txt
flax
optax
chex
# Torch is imported by some modules, but the default PyPI wheel may pull large CUDA deps.
# Install the CPU-only wheel explicitly on TPU VMs:
#   pip install -U torch --index-url https://download.pytorch.org/whl/cpu
# transformers v5 removed `return_tensors="jax"`; keep v4 for JAX return_tensors support.
transformers<5
datasets
tqdm
wandb
math_verify
webdataset
pyyaml
einops
huggingface_hub[hf_transfer]
